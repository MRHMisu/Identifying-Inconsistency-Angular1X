/**
 * Constructor for MVC Group objects, which are placed in the mvc_groups array
 */

function MVCGroup(modelIndex, viewIndex, controllerIndex) {
	this.modelIdx = modelIndex;
	this.viewIdx = viewIndex;
	this.controllerIdx = controllerIndex;
}
